import React from 'react';
import { Layout } from '../components';

export default () =>
  <Layout className="NotFoundPage">
    <h1 className="MainTitle">Cette page reste encore à écrire...</h1>
    <section className="Text">
      <p className="Text-paragraph">Le génie est en moi, mais malheureusement il prend du temps à se manifester.</p>
    </section>
  </Layout>
;
